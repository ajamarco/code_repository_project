<!DOCTYPE html>
<html>
  <head>
    <title>Code Repository</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <script src="https://use.fontawesome.com/releases/v5.0.8/js/all.js"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <%if !!session[:user_id]%>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/">User: <%=User.find(session[:user_id]).name%></a> 
      

      <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <li class="nav-item active">
            <a class="nav-link" href="#">Home</a>
          </li>

          <%if User.find(session[:user_id]).is_admin%>
          <li class="nav-item active">
            <a class="nav-link" href="/users">Show All Users</a>
          </li>
          <%end%>

          <li class="nav-item active">
            <a class="nav-link" href="/posts">Show All Posts</a>
          </li>
          <li class="nav-item active">
            <%= button_to "Create a New Post", {controller: 'posts', action: "new", method: :get}, {class: 'btn btn-outline-success my-2 my-sm-0'} %>
            
          </li>
        </ul>
          <%= button_to "logout", {controller: 'sessions', action: "destroy", method: :post}, {class: 'btn btn-outline-success my-2 my-sm-0'} %>
          
        
      </div>
    </nav>
  <%end%>

    <%= yield %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
  </body>
</html>
