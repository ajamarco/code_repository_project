<% if flash[:errors] %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(flash["errors"].count, "error") %>
      occurred:
    </h2>
    <ul>
    <% flash["errors"].each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>

<% end %>