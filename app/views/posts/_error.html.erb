<% if flash[:errors] %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(flash["errors"].count , "error") %>
      occurred:
    </h2>
 
    <% flash["errors"].each do |msg| %>
      <%= "#{msg}"%><br>
    <% end %>
 
  </div>

<% end %>